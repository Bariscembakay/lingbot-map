# VBR (Vision Benchmark in Rome) — lingbot_map trajectory evaluation.
workspace: /group/compact-3dmem/campaigns/lingbot_map/vbr/default

datasets:
  - vbr

methods:
  - lingbot_map

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: false
