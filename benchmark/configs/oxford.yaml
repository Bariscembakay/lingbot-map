# Oxford Spires — lingbot_map trajectory evaluation.
workspace: /group/compact-3dmem/campaigns/lingbot_map/oxford_spires/sparse_s12

datasets:
  - oxford

methods:
  - lingbot_map

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: true
    vis: true
    aggregation: both
  depth:
    enable: false
  points:
    enable: false
