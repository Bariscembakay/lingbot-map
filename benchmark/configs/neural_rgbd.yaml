# Neural RGB-D — lingbot_map point-cloud evaluation.
# Indoor sequences with GT depth + mesh for surface reconstruction.
workspace: /group/compact-3dmem/campaigns/lingbot_map/neural_rgbd/default

datasets:
  - neural_rgbd

methods:
  - lingbot_map

evaluation:
  traj:
    enable: false
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: true
    vis: false
    icp_threshold: 0.1
