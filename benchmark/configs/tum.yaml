# TUM RGB-D — lingbot_map trajectory evaluation.
# Freiburg1 sequences; RGB frames associated with the nearest GT pose.
workspace: /group/compact-3dmem/campaigns/lingbot_map/tum/default

datasets:
  - tum

methods:
  - lingbot_map

evaluation:
  traj:
    enable: true
    vis: true
  auc:
    enable: false
  depth:
    enable: false
  points:
    enable: false
